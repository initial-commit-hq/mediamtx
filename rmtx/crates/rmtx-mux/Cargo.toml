[package]
name = "rmtx-mux"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimal MPEG-TS muxing for HLS and recording (Phase 3)"

[dependencies]
bytes = { workspace = true }

[dev-dependencies]
bytes = { workspace = true }
